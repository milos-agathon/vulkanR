# Makevars for Windows builds: call rextendr to build Rust crate
all:
	"$(R_HOME)/bin$(R_ARCH_BIN)/Rscript.exe" -e "rextendr::rust_build()"

clean:
	- "$(R_HOME)/bin$(R_ARCH_BIN)/Rscript.exe" -e "rextendr::rust_clean()"
